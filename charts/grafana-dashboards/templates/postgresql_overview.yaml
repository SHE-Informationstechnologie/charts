{{- with .Values.grafanaDashboard.postgres -}}
{{- if .enabled }}

apiVersion: v1
data:
  "postgresql_overview.json": |
    {{ $.Files.Get "dashboards/postgresql_overview.json" | fromJson | toJson | indent 4 }}
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  name: grafana-dashboard-postgresql-overview
  namespace: monitoring

---
{{- end }}
{{- end }}
