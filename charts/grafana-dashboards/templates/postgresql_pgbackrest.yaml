{{- with .Values.grafanaDashboard.postgres -}}
{{- if .enabled }}

apiVersion: v1
data:
  "postgresql_pgbackrest.json": |
    {{ $.Files.Get "dashboards/postgresql_pgbackrest.json" | fromJson | toJson | indent 4 }}
kind: ConfigMap
metadata:
  labels:
    grafana_dashboard: "1"
  name: grafana-dashboard-postgresql-pgbackrest
  namespace: monitoring

---
{{- end }}
{{- end }}
